# Runtime deps. For Gemma fine-tuning, also install:
#   pip install -r requirements-train.txt

streamlit>=1.36
pandas>=2.2
plotly>=5.20
openpyxl>=3.1
gdown>=5.2
requests>=2.32
soundfile>=0.12

# Transcription (CPU-friendly, default backend)
faster-whisper>=1.0

# Database / storage
supabase>=2.7

# Embeddings + semantic search
sentence-transformers>=3.0
numpy>=1.26

# LLM judge — local Gemma 4 path
ollama>=0.3              # CPU-friendly via Ollama daemon (default)
torch>=2.3               # HF transformers path (GPU / fine-tuning)
transformers>=4.43
accelerate>=0.33
peft>=0.12

# Misc
python-dotenv>=1.0
pydantic>=2.7
