# API service deps. Run from repo root with:
#   pip install -r api/requirements.txt
# (also installs the existing src/ runtime deps below)

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7

# pipeline.py + db.py + transcribe.py + embed.py + score.py imports
-r ../requirements.txt
